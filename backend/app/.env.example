# Database configuration
DATABASE_URL=sqlite:///./database.db

# Optionally, you might have other settings like:
# DATABASE_URL=postgresql://username:password@localhost/mydatabase
# DATABASE_URL=mysql://username:password@localhost/mydatabase

# Other environment-specific settings
SECRET_KEY=mysecretkey
DEBUG=True
HOST=localhost
PORT=8003

# API keys or other credentials (ensure these are not committed to version control)
API_KEY=your_api_key_here
